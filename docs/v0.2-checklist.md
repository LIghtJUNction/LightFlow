# LightFlow v0.2 Checklist

This checklist tracks the work needed to turn the current backend MVP into a
release-ready backend foundation. Keep the scope focused on stable contracts,
runtime verification, and one small UI-enabling slice.

Legend:

- `[x]` verified in this repository.
- `[ ]` not complete.

## Release Cleanliness

- [x] `cargo fmt --check` passes.
- [x] `cargo clippy --all-targets -- -D warnings` passes.
- [x] `cargo test` passes with default features.
- [x] `lfw release check` reports the release artifact, changelog document,
  source-change review gate, configurable selected workflow loop, and command
  gates, with `--apply` reserved for executing the expensive commands.
- [x] `lfw loop projects`, `lfw dev check`, `lfw release check`,
  `GET /loop/projects`, `GET /release`, MCP `lightflow.loop.projects`,
  `lightflow.release.check`, `lightflow://loop/projects`, and
  `lightflow://release` expose project config validity, config errors, and
  repair commands instead of failing before a structured report can be
  returned.
- [x] Current security and run-history hardening changes are committed.
- [x] `CHANGELOG.md` records user-visible CLI, API, workflow, runtime, known
  limitation, and migration changes.

## Backend Contract

- [x] OpenAPI paths and schemas match the actual HTTP server behavior.
  `server::tests::openapi_paths_match_http_routes` verifies path parity, and
  `server::tests::live_http_responses_match_openapi_required_fields` validates
  live endpoint bodies against OpenAPI component required fields.
- [x] `lfw info`, `lfw help`, `/nodes`, `/models`, `/runs`, and `/artifacts`
  expose the same workflow/runtime vocabulary through `ApiService`, node-card
  tests, server history tests, and CLI integration tests.
- [x] Path and id segment validation is covered for run history, batch runs,
  patch registry entries, and workflow writes.
- [x] Error responses are structured enough for CLI, MCP, and future editor
  clients. HTTP errors now include `error`, `code`, `message`, and `status`.

## Runtime Verification

- [x] Default preview/mock executors are tested and documented as
  local/demo-safe.
- [x] `--features rig` is tested against at least one mock/local provider path
  and one configured OpenAI-compatible provider path with
  `cargo test --features rig --test llm_rig`.
- [x] `LIGHTFLOW_FLUX_RUNNER` is tested with the external runner contract.
- [x] `--features flux-native` build requirements and supported platforms are
  documented after local verification with `cargo check --features flux-native`.
- [x] Model sync and locked-run paths are tested for missing, present,
  approval-required, and incompatible-format model artifacts.

## Minimal UI Slice

- [x] `lfw serve` can back a read-only node catalog view using `/nodes`.
- [x] A node detail view can display inputs, outputs, model requirements,
  runtime availability, and validation status from `/nodes/{workflow_id}`.
- [x] A run history view can read `/runs`, `/runs/{run_id}`, and
  `/runs/{run_id}/events`.
- [x] No graph canvas is started until the read-only API-backed slice works.
  `LightFlowUI` stays a static backend-backed client for node, model, run,
  artifact, patch, loop, publish, and release-readiness inspection.

## Documentation

- [x] README has one tested quickstart for `lfw init`, `lfw new`, `lfw run`,
  and `lfw serve`.
- [x] `docs/workflow-development.md` has one tested runtime-backed workflow
  example.
- [x] Standard workflow agent skills match their current input/output
  contracts through `lfw node test` coverage in
  `tests/standard_workflow_skills.rs`.
- [x] Runtime docs clearly label preview, mock, external, native, and reserved
  executor paths.

## Completion Evidence

- `lfw release check`
- `cargo fmt --check`
- `cargo clippy --all-targets -- -D warnings`
- `cargo test`
- `cargo test --features rig --test llm_rig`
- `cargo check --features flux-native`
