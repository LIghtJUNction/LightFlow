---
name: lightflow-b
description: Use the lightflow.b ABC example leaf workflow.
version: 0.1.0
---

# lightflow.b

`lightflow.b` is a leaf workflow with one `value` text input and one `value`
text output.

```bash
lfw run lightflow.b -i value=hello
```
