---
name: lightflow-c
description: Use the lightflow.c ABC example leaf workflow.
version: 0.1.0
---

# lightflow.c

`lightflow.c` is a leaf workflow with one `value` text input and one `value`
text output.

```bash
lfw run lightflow.c -i value=hello
```
