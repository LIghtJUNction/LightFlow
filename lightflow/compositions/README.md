# Composition Metadata

Each composition is a self-contained Rust asset file in this directory.

Use `lightflow/compositions/<composition_id>.rs`. The file must contain both metadata and executable composition definition.
