# Model Assets

Model assets are self-contained Rust files under this directory.

Each file defines the alias, provider/format hints, capabilities, license notes, artifact expectations, and source hints together.

Commit model asset files here. Keep heavyweight model weights in external caches or provider-managed stores.
